apply plugin: 'java'

repositories {
    repositories {
        maven{ url "http://${nexus}:8080/nexus/content/groups/public/" }
    }
}

dependencies {
    testCompile 'junit:junit:4.8.2'
}

test {
    maxParallelForks = 5
    forkEvery = 50
}
